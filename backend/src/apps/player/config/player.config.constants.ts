export enum PlayerConfigConstants {
    API_TAG = 'Player Operations',
    CONTROLLER_PREFIX = 'player',
    SEARCH_PLAYERS_BY_GAME = 'game',
    SEARCH_BY_USER = 'user',
    PULL_CARD_FROM_DECK = 'pull-from-deck',
    PULL_CARD_FROM_DISCARDED = 'pull-from-discarded',
}